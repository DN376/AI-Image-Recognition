###
# This program will compress the images & run the interpreter
###
from pytest import skip
import Interpreter
#
# STEP 1: compress the images into an 8x8 list
# each entry will be a double from 0 to 1 (white to black)
# 
compressedImage = []
def compressSingleLetter(image_Letter): #compresses a single letter into 8x8 matrix
    compressedLetter = [] #whatever compreses the image goes here
    compressedImage.append(compressedLetter)
    return "test!"

def compressWord(image_Word, numletters): # compresses a word (no spaces) to a list of compressed letters
    #find someway to section off letters
    skip

def compressPhrase(image, numWords):

    skip
